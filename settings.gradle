dependencyResolutionManagement {
    repositoriesMode.set(RepositoriesMode.FAIL_ON_PROJECT_REPOS)
    repositories {
        google()
        mavenCentral()
        mavenLocal()
        maven { url 'https://jitpack.io' }
        jcenter() // Warning: this repository is going to shut down soon
        maven {
            url uri('repo')
        }
    }
}
rootProject.name = "BadgeView"
include ':app'
include ':lirary'
include ':annotation'
include ':compiler'
